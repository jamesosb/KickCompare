<%= form_for(@kick) do |f| %>
  <% if @kick.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kick.errors.count, "error") %> prohibited this kick from being saved:</h2>

      <ul>
      <% @kick.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :product_type %><br>
    <%= f.text_field :product_type %>
  </div>
  <div class="field">
    <%= f.label :manufacturer %><br>
    <%= f.text_field :manufacturer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
